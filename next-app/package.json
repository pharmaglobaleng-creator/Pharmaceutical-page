{
  "name": "pharmaglobaleng-next",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/prepare-catalogs.mjs && node scripts/optimize-manesty.mjs images && next dev",
    "build": "node scripts/prepare-catalogs.mjs && node scripts/optimize-manesty.mjs images && next build",
    "build:safe": "npm run build && node scripts/assemble-catalog-export.mjs && node scripts/validate-catalog-export.mjs && node scripts/optimize-manesty.mjs details",
    "test:catalog": "playwright test",
    "stage:catalog": "node scripts/stage-manesty-export.mjs",
    "start": "next start"
  },
  "dependencies": {
    "cheerio": "1.2.0",
    "next": "16.3.4",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "sharp": "0.34.5"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
