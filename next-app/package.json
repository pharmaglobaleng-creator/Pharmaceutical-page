{
  "name": "pharmaglobaleng-next",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/prepare-catalogs.mjs && next dev",
    "build": "node scripts/prepare-catalogs.mjs && next build",
    "build:safe": "npm run build && node scripts/assemble-catalog-export.mjs && node scripts/validate-catalog-export.mjs",
    "test:catalog": "playwright test",
    "stage:catalog": "node scripts/stage-catalog-export.mjs",
    "start": "next start"
  },
  "dependencies": {
    "next": "16.3.4",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "cheerio": "1.2.0",
    "sharp": "0.34.5"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
